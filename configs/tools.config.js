export const toolsConfig = {
    MOVE_TOOL: 'moveTool', // Toggles shape dragging
    SELECT_TOOL: 'selectTool', // Toggles shape select (creates outline of the active shape & changes background on click)
    CLOSEST_POINT_TOOL: 'closestPointTool', // FIXME [WIP] -> doesn't work yet, been bashing my head for the solution for far too long
    EXPORT_TOOL: 'exportTool', // Exports the data necessary to reconstruct the state of the project to console
}